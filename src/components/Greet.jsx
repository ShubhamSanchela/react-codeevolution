import React from 'react'

const Greet = props => {
return(
    <div>
        {/* <h1>Hello {props.name} with Surname : {props.surname}</h1>
        {props.children} */}
        Regular Component {this.props.name}
    </div>
)
}
export default Greet;